<%= render 'search' %>

<h1 id="register">
  <% if @search_query.present? %>
    <%= t '.search_results' %>
  <% else %>
    <%= t '.title' %>
  <% end %>
</h1>

<%= render @rooms %>
<%= paginate @rooms %>
